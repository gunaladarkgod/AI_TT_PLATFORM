# 开发协作要求

- 每次改动前先阅读 `README.md`、`docs/v1.0/技术说明文档.md`、`docs/v1.0/GitHub协作流程.md`，以及本次功能相关文档。
- 按协作规范从 `develop` 新建独立分支，采用 `feature/`、`fix/`、`docs/` 等对应名称；检查工作区，保留无关改动。
- 完成必要构建与功能验证后，仅显式暂存本次相关文件并在本地提交。
- 不推送、不创建远程 PR、不发送 GitHub 消息；用户会手动推送。后续用户明确授权时再执行远程操作。
- 不提交运行产物、数据集、权重、日志或个人配置。
